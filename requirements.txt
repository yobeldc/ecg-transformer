streamlit==1.37.0

# NumPy: allow 2.x so it's compatible with Python 3.13
numpy>=2.0

pandas
opencv-python-headless

# Torch version that *does* have wheels for Python 3.13 on Streamlit Cloud
torch==2.9.1

matplotlib
